<%= form_with(model: like) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= form.text_field("reason") %></p>
    <p><%= form.text_field("request") %></p>
    <p><%= form.text_field("source_user_id") %></p>
    <p><%= form.text_field("destination_user_id") %></p>
    <p><%= form.submit %>
  </div>
<% end %>
